<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.peyton.plum.mall.mapper.ShopSkuValueMapper">
	<resultMap id="ResultShopSkuValueMap" type="cn.peyton.plum.mall.pojo.ShopSkuValue">
		<id column="id" property="id" jdbcType="BIGINT"/>
		<result column="sku_id" property="skuId" jdbcType="BIGINT"/>
		<result column="sku_value" property="skuValue" jdbcType="VARCHAR"/>
		<result column="seq" property="seq" jdbcType="INTEGER"/>
	</resultMap>

	<sql id="Column_tb_shop_sku_value_List">
		`id`,`sku_id`,`sku_value`,`seq`
	</sql>

	<insert id="insertSelective" parameterType="cn.peyton.plum.mall.pojo.ShopSkuValue" keyColumn="id" keyProperty="id" useGeneratedKeys="true">
		insert into `tb_shop_sku_value`
		<trim prefix="(" suffix=")" suffixOverrides=",">
			<if test="skuId != null">`sku_id`,</if>
			<if test="skuValue != null">`sku_value`,</if>
			<if test="seq != null">`seq`,</if>
		</trim>
		<trim prefix="values (" suffix=")" suffixOverrides=",">
			<if test="skuId != null">#{skuId},</if>
			<if test="skuValue != null">#{skuValue},</if>
			<if test="seq != null">#{seq},</if>
		</trim>
	</insert>

	<delete id="deleteByPrimaryKey" parameterType="java.lang.Long">
		delete from `tb_shop_sku_value`
		where `id` = #{id}
	</delete>

	<update id="updateByPrimaryKeySelective" parameterType="cn.peyton.plum.mall.pojo.ShopSkuValue">
		update `tb_shop_sku_value`
		<set>
			<if test="skuId != null">`sku_id` = #{skuId},</if>
			<if test="skuValue != null">`sku_value` = #{skuValue},</if>
			<if test="seq != null">`seq` = #{seq},</if>
		</set>
		where id = #{id}
	</update>

	<select id="selectByPrimaryKey" resultMap="ResultShopSkuValueMap"  parameterType="java.lang.Long">
		select
		<include refid="Column_tb_shop_sku_value_List"/>
		from `tb_shop_sku_value`
		where `id` = #{id}
	</select>

	<!-- new create method   -->	

	<select id="selectByAllOrKeyword" resultMap="ResultShopSkuValueMap"  parameterType="map">

	</select>

	<select id="selectByObj" resultMap="ResultShopSkuValueMap"  parameterType="map">

	</select>

	<select id="count" resultType = "java.lang.Integer" parameterType="java.lang.String">

	</select>

	<select id="isRename" resultType = "java.lang.Integer" parameterType="cn.peyton.plum.mall.pojo.ShopSkuValue">

	</select>

	<update id="upStatus" parameterType="cn.peyton.plum.mall.pojo.ShopSkuValue">

	</update>

</mapper>
