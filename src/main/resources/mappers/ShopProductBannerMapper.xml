<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.peyton.plum.mall.mapper.ShopProductBannerMapper">
	<resultMap id="ResultShopProductBannerMap" type="cn.peyton.plum.mall.pojo.ShopProductBanner">
		<id column="id" property="id" jdbcType="BIGINT"/>
		<result column="product_id" property="productId" jdbcType="BIGINT"/>
		<result column="url" property="url" jdbcType="VARCHAR"/>
		<result column="create_time" property="createTime" jdbcType="INTEGER"/>
	</resultMap>

	<sql id="Column_tb_shop_product_banner_List">
		`id`,`product_id`,`url`,`create_time`
	</sql>

	<insert id="insertSelective" parameterType="cn.peyton.plum.mall.pojo.ShopProductBanner" keyColumn="id" keyProperty="id" useGeneratedKeys="true">
		insert into `tb_shop_product_banner`
		<trim prefix="(" suffix=")" suffixOverrides=",">
			<if test="productId != null">`product_id`,</if>
			<if test="url != null">`url`,</if>
			<if test="createTime != null">`create_time`,</if>
		</trim>
		<trim prefix="values (" suffix=")" suffixOverrides=",">
			<if test="productId != null">#{productId},</if>
			<if test="url != null">#{url},</if>
			<if test="createTime != null">#{createTime},</if>
		</trim>
	</insert>

	<delete id="deleteByPrimaryKey" parameterType="java.lang.Long">
		delete from `tb_shop_product_banner`
		where `id` = #{id}
	</delete>

	<update id="updateByPrimaryKeySelective" parameterType="cn.peyton.plum.mall.pojo.ShopProductBanner">
		update `tb_shop_product_banner`
		<set>
			<if test="productId != null">`product_id` = #{productId},</if>
			<if test="url != null">`url` = #{url},</if>
			<if test="createTime != null">`create_time` = #{createTime},</if>
		</set>
		where id = #{id}
	</update>

	<select id="selectByPrimaryKey" resultMap="ResultShopProductBannerMap"  parameterType="java.lang.Long">
		select
		<include refid="Column_tb_shop_product_banner_List"/>
		from `tb_shop_product_banner`
		where `id` = #{id}
	</select>

	<!-- new create method   -->	

	<select id="selectByAllOrKeyword" resultMap="ResultShopProductBannerMap"  parameterType="map">

	</select>

	<select id="selectByObj" resultMap="ResultShopProductBannerMap"  parameterType="map">

	</select>

	<select id="count" resultType = "java.lang.Integer" parameterType="java.lang.String">

	</select>

	<select id="isRename" resultType = "java.lang.Integer" parameterType="cn.peyton.plum.mall.pojo.ShopProductBanner">

	</select>

	<update id="upStatus" parameterType="cn.peyton.plum.mall.pojo.ShopProductBanner">

	</update>

</mapper>
